// Note: With @tailwindcss/vite plugin, PostCSS config is not needed
// If you need PostCSS plugins, install @tailwindcss/postcss instead
// For now, this file is kept for compatibility but may not be used
module.exports = {
    plugins: {
        // Using @tailwindcss/vite plugin in vite.config.js instead
        autoprefixer: {},
    },
}
